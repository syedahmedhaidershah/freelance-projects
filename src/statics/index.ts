import { appRoutes as AppRoutes } from './appRoutes';
import { uxConfig as UXConfig } from './uxConfig';
import { snackBars as SnackBars } from './snackBars';

export const Statics = {
    AppRoutes,
    UXConfig,
    SnackBars,
}
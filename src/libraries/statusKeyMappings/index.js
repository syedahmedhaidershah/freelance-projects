const ErrorKeys = require('./errors/errors.keys');
const ErrorMessages = require('./errors/errors.strings');

const NotificationKeys = require('./notification/notification.keys');
const NotificationMessages = require('./notification/notification.strings');

const SuccessKeys = require('./success/success.keys');
const SuccessMessages = require('./success/success.strings');

module.exports = {
  ErrorKeys,
  ErrorMessages,

  NotificationKeys,
  NotificationMessages,

  SuccessKeys,
  SuccessMessages,
};

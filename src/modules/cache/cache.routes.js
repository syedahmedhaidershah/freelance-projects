const router = require('express').Router();
const notificationCtrl = require('./cache.controller');

// router.post('/store', notificationCtrl.createNotification);

// router.post('/getAll', notificationCtrl.getAll);

// router.post('/markRead', notificationCtrl.markRead);

module.exports = router;
